{% extends 'AppBundle::base.html.twig' %}

{% block body %}
<div class="container">
    {# app/Resources/views/default/new.html.twig #}
    {{ form_start(form, {'attr': {'class': 'blogger'} }) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</div>
<a href="{{ url('task_list') }}">view</a> all tasks.
{% endblock %}

